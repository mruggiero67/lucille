"""Shared helpers used across lucille scripts.

Submodules:
  - config:  ``load_yaml_config`` — YAML loading with optional validation
  - logging: ``setup_logging``    — one standard log format
  - paths:   ``DEBRIS_DIR`` and friends — canonical output locations
"""
