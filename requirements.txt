# Runtime dependencies for lucille.
#
# Development-only tools (pytest, black, coverage) live in requirements-dev.txt.
# Everything here is imported by code under lucille/ (directly, or transitively
# in a load-bearing way — e.g. openpyxl for df.to_excel).
#
# To regenerate exact pins after upgrading:
#   python -m venv .venv-fresh && source .venv-fresh/bin/activate
#   pip install -r requirements.txt && pip freeze > requirements.lock.txt

boto3==1.42.97
matplotlib==3.9.4
numpy==2.0.2
openpyxl==3.1.2
pandas==2.3.0
python-dateutil==2.9.0.post0
PyYAML==6.0.2
requests==2.32.3
seaborn==0.13.2
