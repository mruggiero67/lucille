# Runtime dependencies for lucille.
#
# The pins themselves now live in pyproject.toml (single source of truth).
# This file exists only so `pip install -r requirements.txt` still works
# for anyone with that in their muscle memory.
#
# Install with:
#   pip install -r requirements.txt   # runtime only
#   pip install -r requirements-dev.txt  # + pytest/black/coverage

-e .
