# Development dependencies for lucille.
#
# The pins themselves now live in pyproject.toml's [project.optional-dependencies].dev.
# This file is a thin pointer that installs both runtime and dev extras.
#
# Install with:
#   pip install -r requirements-dev.txt

-e .[dev]
